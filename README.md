# RenderMage Downloads

Public download host for RenderMage RM_Sync plugin binaries.

Current stable artifacts live in:

- `plugins/latest/sketchup-rm-sync.rbz`
- `plugins/latest/blender-rm-sync.zip`
- `plugins/latest/archicad-rm-sync-win.zip`
- `plugins/latest/archicad-rm-sync-mac.zip`

Versioned copies are also stored in:

- `plugins/v1.0.0/`
